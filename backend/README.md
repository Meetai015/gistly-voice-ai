# FastAPI Backend

Install dependencies:
```bash
pip install -r requirements.txt
```

Run the server:
```bash
uvicorn main:app --reload
```

Run tests:
```bash
pytest
```

Setup pre-commit hooks:
```bash
pre-commit install
```
